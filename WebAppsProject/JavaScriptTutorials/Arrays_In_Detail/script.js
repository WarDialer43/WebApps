/* Arrays (DataType Object): A specail data structure used to create an ordered collection
of data such as users, items, elimants etc.. */

// const month = ['January', 'February', 'March', 'April'];

// console.log(month);

// console.log(month.length);

// for(let i = 0; i < month.length; i++) {
// console.log(month[i]);
// }
// const values = [
// 'Apples',
// { name: 'shitter' },
// true,
// () => {

// }

/* Array pre made functions:



1) Array push : Adds a new element containing the entered value to the end of the array

2) Array pop: Deletes the last element of an array

3) Array shift: Deletes the first element of an array

4) Array Unshift: Add a new value to the start of an array

5) Array splice :  splice values into the new array, its first peram
determinds where the new elements are placed and the second how many after that point
should be deleted before placement and then all the perams after are the 
values that you want to push in. It adds/removes valaues from any postion in the array

6) Array slice: Make a new variable that contains every element in the previous array.
copies certain parts of an array into a newely created array.
*/
//  const names = ['bob', 'jeof', 'rob', 'den' ];

//  names.push('lizz');
//  names.pop();
//  names.shift();
//  names.unshift('jake');
//  names.splice(2, 0, 'pete', 'alan', );
//  const noOneLikesBob = names.slice(1);
    // console.log(noOneLikesBob);
//  console.log(names);




